define(
    [
        "../_base"
    ],
    function(scrollorama) {
        
        // define the package structure
        scrollorama.structures = { };
        
        return scrollorama.structures;
    }
);
