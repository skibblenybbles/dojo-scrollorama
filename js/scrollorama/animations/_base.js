define(
    [
        "../_base"
    ],
    function(scrollorama) {
        
        // define the package structure
        scrollorama.animation = { };
        return scrollorama.animation;
    }
);
